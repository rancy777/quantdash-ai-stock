## Integrations

这里放外部通讯与通知渠道的接入层。

建议约定：

- `feishu/`：飞书机器人、消息推送、回调处理
- `wecom/`：企业微信
- `dingtalk/`：钉钉
- `shared/`：多渠道共用的签名、消息模板、路由、重试逻辑

当前原则：

- 渠道接入层放这里
- 市场数据读取仍复用上层 `scripts/` 里的共享数据模块
- 不要在渠道脚本里重复实现情绪周期或市场数据口径
